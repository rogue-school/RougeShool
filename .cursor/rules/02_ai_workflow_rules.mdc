---
alwaysApply: true
description: "AI code generation workflow: existing code analysis, reusability evaluation, quality verification"
---

# RougeShool AI Workflow Rules

## 🔄 Required Workflow

### Step 1: Code Analysis (NEVER SKIP)
```
🚨 FORBIDDEN: Generate new code without reviewing existing code
✅ REQUIRED: Complete existing code review before generation

Workflow:
1. codebase_search → Find similar functionality
2. grep → Verify exact matches
3. read_file → Analyze existing code in detail
4. Evaluate reuse strategy
5. Only create new code if absolutely necessary
```

### Step 2: During Code Generation
```
✅ Required Implementation:
├── Exception handling (null checks, validation)
├── Korean logging (GameLogger)
├── XML documentation (public APIs)
├── Consistency with existing code
└── Follow project architecture patterns
```

### Step 3: Post-Generation Verification
```
🔍 Quality Verification Checklist:
├── read_lints → 0 warnings, 0 errors
├── Consistency with existing code
├── Exception handling implemented
├── Korean logging implemented
└── XML documentation complete
```

## 🛠️ Tool Usage Rules

### Search Tools (Ordered)
```
1. codebase_search (broad search)
   ├── Semantic code search
   ├── Find similar functionality
   └── Identify related systems

2. grep (exact matching)
   ├── Exact symbol/string search
   ├── Find specific patterns
   └── Verify exact locations

3. read_file (detailed analysis)
   ├── Understand code context
   ├── Check implementation details
   └── Analyze dependencies
```

### Edit Tools
```
Small edits: search_replace
├── Single file changes
├── Minimal changes with clear context
└── Precise code insertion/modification

Multiple edits: MultiEdit
├── Multiple files simultaneously
├── Related changes across files
└── Consistent changes applied
```

### Parallelization
```
Independent operations:
├── 2+ independent search/read tasks
├── Execute in parallel for efficiency
└── Reduce response time
```

## 📋 Quality Verification

### Compilation Verification
```
Required checks:
├── 0 compilation warnings
├── 0 compilation errors
├── 0 linter warnings
└── 0 runtime errors
```

### Code Quality Verification
```
Required checks:
├── Exception handling complete
├── Korean logging complete
├── XML documentation complete
├── Consistency with existing code
└── Architecture patterns followed
```

### Performance Verification
```
Recommended checks:
├── Minimize Update() usage
├── Minimize memory allocation
├── Use DOTween Pro
└── Apply Zenject DI
```

## 🚫 Strict Prohibitions

### Code Generation Prohibitions
```
NEVER generate:
├── Example code (only when explicitly requested)
├── Dummy data
├── Duplicate functionality
├── AnimationSystem-related code
├── Game-specific reference comments (e.g., "Slay the Spire style")
├── AI-generated code mentions (e.g., "AI generated")
├── Change notification comments (e.g., "수정됨", "FIXED", "ADDED", "Modified here")
├── Explanatory comments for user (e.g., "이 부분을 수정했습니다", "성능 개선을 위해 변경")
└── Incomplete explanatory code
```

### Workflow Violations
```
NEVER skip:
├── Existing code review step
├── Reusability evaluation step
├── Quality verification step
└── Compilation verification step
```

## ✅ Success Criteria

### Code Generation Success
```
Success conditions:
├── Existing code reused/extended first
├── 0 compilation warnings/errors
├── Complete exception handling
├── Korean logging implemented
├── XML documentation complete
└── Consistency with existing code maintained
```

### Workflow Success
```
Success conditions:
├── All required steps completed
├── All required verifications passed
├── All prohibitions followed
└── All success criteria met
```
