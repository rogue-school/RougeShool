---
alwaysApply: true
description: "AI-사용자 협업을 위한 핵심 개발 원칙. SOLID 원칙, 아키텍처 패턴, 성능 최적화, 코드 품질 기준을 포함합니다."
---

# RougeShool 핵심 개발 규칙 (Core Development Rules)

> 목적: AI-사용자 협업을 통한 고품질 코드 생성. SOLID 원칙, 아키텍처 패턴, 성능 최적화를 기반으로 한 개발 표준.

## 🏆 핵심 원칙 (SOLID)

### 1. 단일 책임 원칙 (SRP)
```csharp
// 좋은 예: 하나의 책임만 가짐
public class CardValidator
{
    public bool CanPlayCard(SkillCard card, IPlayer player)
    {
        return card != null && card.IsAvailable && player.CurrentMana >= card.ManaCost;
    }
}

// 나쁜 예: 여러 책임을 가짐
public class CardManager
{
    public void ProcessCard(SkillCard card)
    {
        // 검증, 오디오 재생, 로깅, UI 업데이트 등 여러 책임
    }
}
```

### 2. 개방-폐쇄 원칙 (OCP)
```csharp
// 확장에는 열려있고 수정에는 닫혀있음
public abstract class CardEffect
{
    public abstract void Apply(IPlayer player);
}

public class DamageEffect : CardEffect
{
    public override void Apply(IPlayer player) { /* 데미지 로직 */ }
}
```

### 3. 의존성 역전 원칙 (DIP)
```csharp
// 추상에 의존
public class CardProcessor
{
    private readonly ICardValidator validator;
    private readonly IAudioManager audioManager;
    
    public CardProcessor(ICardValidator validator, IAudioManager audioManager)
    {
        this.validator = validator ?? throw new ArgumentNullException(nameof(validator));
        this.audioManager = audioManager ?? throw new ArgumentNullException(nameof(audioManager));
    }
}
```

## 🔥 AI-사용자 협업 최우선 규칙

### 1. 기존 코드 우선 원칙
```markdown
🚨 절대 금지: 기존 코드 검토 없이 새 코드 생성
✅ 필수: 모든 코드 생성 전 기존 코드 리뷰 완료

AI 작업 순서:
1. codebase_search로 유사 기능 검색
2. grep으로 정확한 매칭 확인  
3. read_file로 기존 코드 분석
4. 기존 코드 재사용/확장 방안 도출
5. 신규 생성이 불가피한 경우에만 새 코드 작성
```

### 2. 예외 처리 강화
```csharp
// 완전한 예외 처리
public bool ProcessCard(SkillCard card, IPlayer player)
{
    if (card == null)
    {
        GameLogger.LogError("카드가 null입니다", GameLogger.LogCategory.Error);
        throw new ArgumentNullException(nameof(card));
    }
    
    try
    {
        return ExecuteCardLogic(card, player);
    }
    catch (Exception ex)
    {
        GameLogger.LogError($"카드 처리 중 오류: {ex.Message}", GameLogger.LogCategory.Error);
        throw new InvalidOperationException("카드 처리 실패", ex);
    }
}
```

### 3. 한국어 표준
```markdown
모든 텍스트 요소 한국어 표준:
- 주석/문서/로그: 한국어
- Inspector 필드 라벨: 한국어  
- XML 문서화: 한국어
- 에러 메시지: 한국어
```

### 4. Unity Inspector 한글화 필수 규칙
```csharp
// ✅ 올바른 Inspector 한글화
public class MyComponent : MonoBehaviour
{
    [Header("전투 설정")]
    [SerializeField] private int maxHealth = 100;
    [SerializeField] private float attackPower = 10.0f;
    
    [Header("UI 설정")]
    [SerializeField] private GameObject healthBarPrefab;
    [SerializeField] private AudioClip buttonClickSound;
    
    [Space(10)]
    [Tooltip("플레이어의 최대 체력입니다")]
    [SerializeField] private int playerMaxHealth = 100;
    
    [Tooltip("공격력 배율입니다 (1.0 = 기본값)")]
    [Range(0.1f, 5.0f)]
    [SerializeField] private float damageMultiplier = 1.0f;
}

// ❌ 잘못된 Inspector 한글화 (영어 필드명)
public class BadComponent : MonoBehaviour
{
    [SerializeField] private int maxHealth = 100; // 영어 필드명
    [SerializeField] private float attackPower = 10.0f; // 영어 필드명
}
```

## 📋 핵심 아키텍처 패턴

### 싱글톤 패턴
```csharp
public class MyManager : MonoBehaviour
{
    public static MyManager Instance { get; private set; }
    
    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
```

### Zenject DI 패턴
```csharp
// CoreSystemInstaller 패턴
public class CoreSystemInstaller : MonoInstaller<CoreSystemInstaller>
{
    [SerializeField] private AudioManager audioManager;
    
    public override void InstallBindings()
    {
        Container.Bind<IAudioManager>().FromInstance(audioManager).AsSingle();
        Container.Bind<ICoreSystemInitializable>().To<CoreSystemInitializer>().AsSingle();
    }
}

// 의존성 주입 사용
public class MyController : MonoBehaviour
{
    [Inject] private IAudioManager audioManager;
    
    private void Start()
    {
        audioManager.PlaySFX(sfxClip);
    }
}
```

### DOTween Pro 패턴
```csharp
// 기본 애니메이션 패턴
transform.DOMove(targetPosition, duration)
    .SetEase(Ease.OutQuad)
    .OnComplete(() => GameLogger.LogInfo("이동 완료", GameLogger.LogCategory.UI));

// 체인 애니메이션
Sequence sequence = DOTween.Sequence();
sequence.Append(transform.DOScale(Vector3.one * 1.2f, 0.3f).SetEase(Ease.OutBack))
         .Append(transform.DOScale(Vector3.one, 0.2f).SetEase(Ease.InBack));
```

## ⚡ 성능 최적화 기준

### 메모리 관리
```csharp
// 오브젝트 풀링 활용
public class CardPool : MonoBehaviour
{
    private Queue<SkillCard> availableCards = new Queue<SkillCard>();
    
    public SkillCard GetCard()
    {
        if (availableCards.Count > 0)
        {
            return availableCards.Dequeue();
        }
        return Instantiate(cardPrefab, transform);
    }
    
    public void ReturnCard(SkillCard card)
    {
        card.gameObject.SetActive(false);
        availableCards.Enqueue(card);
    }
}
```

### Unity 최적화
- **Sprite Atlas**: 모든 스프라이트를 아틀라스로 통합
- **2D Physics**: 3D Physics 사용 금지
- **Draw Call**: 배치 처리로 Draw Call 최소화
- **Update() 금지**: 이벤트 기반 처리 필수

## 🚫 절대 금지사항

### 🔴 **아키텍처 위반**
- SOLID 원칙 위반
- 순환 의존성 생성
- 단일 책임 원칙 위반

### 🔴 **성능 위반**
- 매 프레임 FindObjectOfType 호출
- 매 프레임 무거운 계산
- 무제한 오브젝트 생성

### 🔴 **코드 품질 위반**
- 예시 코드 생성 (사용자 명시적 요청 시에만)
- 중복 기능 생성
- 더미 데이터 생성
- 특정 게임 참조 주석 (예: "슬레이 더 스파이어 방식", "League of Legends 스타일")
- AI 생성 코드 언급 (예: "AI가 생성한", "AI 생성")

### 🔴 **AI 협업 위반**
- 기존 코드 검토 없이 새 코드 생성
- 사용자 피드백 무시
- 프로젝트 패턴과 불일치

## ✅ 필수 체크리스트

### 📋 **AI 코드 생성 시 필수 검증**
- [ ] **SOLID 원칙 준수**: 모든 원칙이 적용되었는가?
- [ ] **기존 코드 우선**: 재사용 가능한 코드를 찾았는가?
- [ ] **아키텍처 일관성**: 프로젝트 패턴과 일치하는가?
- [ ] **성능 최적화**: 메모리 할당과 GC 압박을 최소화했는가?
- [ ] **예외 처리**: 모든 예외 상황에 대한 처리가 완료되었는가?
- [ ] **한국어 표준**: 모든 텍스트가 한국어로 작성되었는가?
- [ ] **Inspector 한글화**: Header, Tooltip, 필드명이 한국어로 작성되었는가?
- [ ] **XML 문서화**: public API가 완전히 문서화되었는가?
- [ ] **컴파일 검증**: 경고/오류가 0인가?

### 📋 **코드 품질 검증**
- [ ] **복잡도 제한**: 순환 복잡도 5 이하, 인지 복잡도 7 이하
- [ ] **메모리 효율성**: 오브젝트 풀링과 캐싱 적용
- [ ] **이벤트 기반**: Update() 대신 이벤트 기반 처리
- [ ] **의존성 주입**: Zenject DI 패턴 준수
- [ ] **로깅 표준**: GameLogger를 통한 카테고리별 로깅

## 🔧 도구 사용 순서

1. **탐색**: codebase_search(광역) → grep(정확) → read_file(세부)
2. **편집**: edit_file(소규모) → MultiEdit(다중)
3. **검증**: read_lints로 경고/오류 0 확인
4. **병렬화**: 독립 검색/읽기는 parallel로 동시 실행

## 📝 변경 기록
- 2025-01-27 | Maintainer | Unity Inspector 한글화 규칙 추가 및 체크리스트 강화 | 문서