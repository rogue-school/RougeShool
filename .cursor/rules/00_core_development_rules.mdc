---
alwaysApply: true
description: "RougeShool 프로젝트의 핵심 개발 원칙과 규칙을 정의합니다. 기존 코드 우선 원칙, 예외 처리, 한국어 표준, 아키텍처 패턴, Zenject DI, DOTween Pro 사용법을 포함합니다."
---

# RougeShool 핵심 개발 규칙 (Core Development Rules)

> 목적: AI가 프로젝트를 이해하고 올바른 코드를 생성하기 위한 핵심 규칙. 기존 코드 우선 원칙을 최우선으로 하여 중복을 방지하고 품질을 보장한다.

## 🔥 최우선 규칙 (절대 건너뛰기 금지)

### 1. 기존 코드 우선 원칙 (최우선)
```markdown
🚨 절대 금지: 기존 코드 검토 없이 새 코드 생성
✅ 필수: 모든 코드 생성 전 기존 코드 리뷰 완료
📋 증빙: 기존 코드 재사용 불가 사유 명시

작업 순서:
1. codebase_search로 유사 기능 검색
2. grep으로 정확한 매칭 확인  
3. read_file로 기존 코드 분석
4. 기존 코드 재사용/확장 방안 도출
5. 신규 생성이 불가피한 경우에만 새 코드 작성
```

### 2. 예외 처리 강화 (필수)
```markdown
모든 public 메서드에서:
- null 매개변수 검증 → ArgumentNullException
- 범위/유효성 검증 → ArgumentException  
- 잘못된 상태 검증 → InvalidOperationException
- 한국어 로깅 (GameLogger.Info/Warning/Error)
```

### 3. 한국어 표준 (필수)
```markdown
- 모든 주석/문서/로그: 한국어
- Inspector 필드 라벨: 한국어
- XML 문서화: 한국어
- 에러 메시지: 한국어
```

## 📋 핵심 아키텍처 패턴

### 싱글톤 패턴
```csharp
// 매니저 클래스에만 적용
public class MyManager : MonoBehaviour
{
    public static MyManager Instance { get; private set; }
    
    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
```

### Zenject DI 패턴 (현재 프로젝트 구조)
```csharp
// CoreSystemInstaller 패턴 (전역 시스템)
public class CoreSystemInstaller : MonoInstaller<CoreSystemInstaller>
{
    [SerializeField] private AudioManager audioManager;
    [SerializeField] private SaveManager saveManager;
    
    public override void InstallBindings()
    {
        // 싱글톤 매니저 바인딩 (DontDestroyOnLoad)
        Container.Bind<IAudioManager>().FromInstance(audioManager).AsSingle();
        Container.Bind<ISaveManager>().FromInstance(saveManager).AsSingle();
        
        // 인터페이스 기반 바인딩
        Container.Bind<ICoreSystemInitializable>().To<CoreSystemInitializer>().AsSingle();
    }
}

// CombatInstaller 패턴 (씬별 시스템)
public class CombatInstaller : MonoInstaller
{
    public override void InstallBindings()
    {
        // 씬에서 찾아서 바인딩
        var turnManager = FindFirstObjectByType<TurnManager>();
        Container.Bind<ITurnManager>().FromInstance(turnManager).AsSingle();
        
        // 팩토리 패턴 바인딩
        Container.BindFactory<CombatState, CombatState.Factory>();
    }
}

// 의존성 주입 사용
public class MyController : MonoBehaviour
{
    [Inject] private IAudioManager audioManager;
    [Inject] private ITurnManager turnManager;
    
    private void Start()
    {
        // 주입된 서비스 사용
        audioManager.PlaySFX(sfxClip);
        turnManager.StartSetupPhase();
    }
}
```

### DOTween Pro 패턴 (현재 프로젝트 설정)
```csharp
// CoreSystemInitializer에서 설정된 최적화된 DOTween 사용
// 설정: SetTweensCapacity(500, 100), SafeMode 활성화, OutQuad 기본값

// 기본 애니메이션 패턴
transform.DOMove(targetPosition, duration)
    .SetEase(Ease.OutQuad)
    .OnComplete(() => GameLogger.LogInfo("이동 완료", GameLogger.LogCategory.UI));

// UI 애니메이션 (CanvasGroup 활용)
CanvasGroup canvasGroup = GetComponent<CanvasGroup>();
canvasGroup.DOFade(1f, 0.5f).SetEase(Ease.OutQuad);

// 체인 애니메이션
Sequence sequence = DOTween.Sequence();
sequence.Append(transform.DOScale(Vector3.one * 1.2f, 0.3f).SetEase(Ease.OutBack))
         .Append(transform.DOScale(Vector3.one, 0.2f).SetEase(Ease.InBack))
         .OnComplete(() => GameLogger.LogInfo("체인 애니메이션 완료", GameLogger.LogCategory.UI));
```

## ⚡ 성능 최적화 필수사항

### 메모리 관리
- Update() 최소화
- 불필요한 참조 해제
- 객체 풀링 활용
- DOTween.SetTweensCapacity() 설정

### Unity 2D 최적화
- Sprite Atlas 활용
- 2D Physics만 사용
- Draw Call 최소화
- Canvas Group 활용

## 🚫 절대 금지사항

1. **예시 코드 생성 금지**: 사용자가 명시적으로 요청하지 않는 한 예시 코드 생성하지 않음
2. **중복 기능 생성 금지**: 기존 코드와 동일/유사한 기능 새로 생성하지 않음
3. **AnimationSystem 참조 금지**: 모든 AnimationSystem 관련 코드 제거 완료, Debug.Log로 임시 처리
4. **더미 데이터 금지**: 실제 프로젝트에 사용할 수 없는 더미 데이터 생성하지 않음

## ✅ 필수 체크리스트 (모든 코드 생성 시)

- [ ] 기존 코드 검색 및 분석 완료
- [ ] 기존 코드 재사용/확장 가능성 검토 완료
- [ ] 기존 코드 개선 방안 검토 완료
- [ ] 신규 생성 불가피 사유 명시 완료
- [ ] 예외 처리 구현 완료 (null 체크, 유효성 검증)
- [ ] 한국어 로깅 구현 완료
- [ ] XML 문서화 완료 (public API)
- [ ] 컴파일 경고 0 확인 완료
- [ ] 기존 코드와의 일관성 확인 완료

## 🔧 도구 사용 순서 (필수)

1. **탐색**: codebase_search(광역) → grep(정확) → read_file(세부)
2. **편집**: edit_file(소규모) → MultiEdit(다중)
3. **검증**: read_lints로 경고/오류 0 확인
4. **병렬화**: 독립 검색/읽기는 parallel로 동시 실행

## 📝 변경 기록
- 2025-01-27 | Maintainer | 핵심 규칙 통합 및 기존 코드 우선 원칙 강화 | 문서# RougeShool 핵심 개발 규칙 (Core Development Rules)

> 목적: AI가 프로젝트를 이해하고 올바른 코드를 생성하기 위한 핵심 규칙. 기존 코드 우선 원칙을 최우선으로 하여 중복을 방지하고 품질을 보장한다.

## 🔥 최우선 규칙 (절대 건너뛰기 금지)

### 1. 기존 코드 우선 원칙 (최우선)
```markdown
🚨 절대 금지: 기존 코드 검토 없이 새 코드 생성
✅ 필수: 모든 코드 생성 전 기존 코드 리뷰 완료
📋 증빙: 기존 코드 재사용 불가 사유 명시

작업 순서:
1. codebase_search로 유사 기능 검색
2. grep으로 정확한 매칭 확인  
3. read_file로 기존 코드 분석
4. 기존 코드 재사용/확장 방안 도출
5. 신규 생성이 불가피한 경우에만 새 코드 작성
```

### 2. 예외 처리 강화 (필수)
```markdown
모든 public 메서드에서:
- null 매개변수 검증 → ArgumentNullException
- 범위/유효성 검증 → ArgumentException  
- 잘못된 상태 검증 → InvalidOperationException
- 한국어 로깅 (GameLogger.Info/Warning/Error)
```

### 3. 한국어 표준 (필수)
```markdown
- 모든 주석/문서/로그: 한국어
- Inspector 필드 라벨: 한국어
- XML 문서화: 한국어
- 에러 메시지: 한국어
```

## 📋 핵심 아키텍처 패턴

### 싱글톤 패턴
```csharp
// 매니저 클래스에만 적용
public class MyManager : MonoBehaviour
{
    public static MyManager Instance { get; private set; }
    
    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
```

### Zenject DI 패턴 (현재 프로젝트 구조)
```csharp
// CoreSystemInstaller 패턴 (전역 시스템)
public class CoreSystemInstaller : MonoInstaller<CoreSystemInstaller>
{
    [SerializeField] private AudioManager audioManager;
    [SerializeField] private SaveManager saveManager;
    
    public override void InstallBindings()
    {
        // 싱글톤 매니저 바인딩 (DontDestroyOnLoad)
        Container.Bind<IAudioManager>().FromInstance(audioManager).AsSingle();
        Container.Bind<ISaveManager>().FromInstance(saveManager).AsSingle();
        
        // 인터페이스 기반 바인딩
        Container.Bind<ICoreSystemInitializable>().To<CoreSystemInitializer>().AsSingle();
    }
}

// CombatInstaller 패턴 (씬별 시스템)
public class CombatInstaller : MonoInstaller
{
    public override void InstallBindings()
    {
        // 씬에서 찾아서 바인딩
        var turnManager = FindFirstObjectByType<TurnManager>();
        Container.Bind<ITurnManager>().FromInstance(turnManager).AsSingle();
        
        // 팩토리 패턴 바인딩
        Container.BindFactory<CombatState, CombatState.Factory>();
    }
}

// 의존성 주입 사용
public class MyController : MonoBehaviour
{
    [Inject] private IAudioManager audioManager;
    [Inject] private ITurnManager turnManager;
    
    private void Start()
    {
        // 주입된 서비스 사용
        audioManager.PlaySFX(sfxClip);
        turnManager.StartSetupPhase();
    }
}
```

### DOTween Pro 패턴 (현재 프로젝트 설정)
```csharp
// CoreSystemInitializer에서 설정된 최적화된 DOTween 사용
// 설정: SetTweensCapacity(500, 100), SafeMode 활성화, OutQuad 기본값

// 기본 애니메이션 패턴
transform.DOMove(targetPosition, duration)
    .SetEase(Ease.OutQuad)
    .OnComplete(() => GameLogger.LogInfo("이동 완료", GameLogger.LogCategory.UI));

// UI 애니메이션 (CanvasGroup 활용)
CanvasGroup canvasGroup = GetComponent<CanvasGroup>();
canvasGroup.DOFade(1f, 0.5f).SetEase(Ease.OutQuad);

// 체인 애니메이션
Sequence sequence = DOTween.Sequence();
sequence.Append(transform.DOScale(Vector3.one * 1.2f, 0.3f).SetEase(Ease.OutBack))
         .Append(transform.DOScale(Vector3.one, 0.2f).SetEase(Ease.InBack))
         .OnComplete(() => GameLogger.LogInfo("체인 애니메이션 완료", GameLogger.LogCategory.UI));
```

## ⚡ 성능 최적화 필수사항

### 메모리 관리
- Update() 최소화
- 불필요한 참조 해제
- 객체 풀링 활용
- DOTween.SetTweensCapacity() 설정

### Unity 2D 최적화
- Sprite Atlas 활용
- 2D Physics만 사용
- Draw Call 최소화
- Canvas Group 활용

## 🚫 절대 금지사항

1. **예시 코드 생성 금지**: 사용자가 명시적으로 요청하지 않는 한 예시 코드 생성하지 않음
2. **중복 기능 생성 금지**: 기존 코드와 동일/유사한 기능 새로 생성하지 않음
3. **AnimationSystem 참조 금지**: 모든 AnimationSystem 관련 코드 제거 완료, Debug.Log로 임시 처리
4. **더미 데이터 금지**: 실제 프로젝트에 사용할 수 없는 더미 데이터 생성하지 않음

## ✅ 필수 체크리스트 (모든 코드 생성 시)

- [ ] 기존 코드 검색 및 분석 완료
- [ ] 기존 코드 재사용/확장 가능성 검토 완료
- [ ] 기존 코드 개선 방안 검토 완료
- [ ] 신규 생성 불가피 사유 명시 완료
- [ ] 예외 처리 구현 완료 (null 체크, 유효성 검증)
- [ ] 한국어 로깅 구현 완료
- [ ] XML 문서화 완료 (public API)
- [ ] 컴파일 경고 0 확인 완료
- [ ] 기존 코드와의 일관성 확인 완료

## 🔧 도구 사용 순서 (필수)

1. **탐색**: codebase_search(광역) → grep(정확) → read_file(세부)
2. **편집**: edit_file(소규모) → MultiEdit(다중)
3. **검증**: read_lints로 경고/오류 0 확인
4. **병렬화**: 독립 검색/읽기는 parallel로 동시 실행

## 📝 변경 기록
- 2025-01-27 | Maintainer | 핵심 규칙 통합 및 기존 코드 우선 원칙 강화 | 문서