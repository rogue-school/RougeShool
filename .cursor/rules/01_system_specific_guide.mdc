---
alwaysApply: true
description: "RougeShool 프로젝트의 각 시스템별 핵심 사용법과 주의사항을 제공합니다. CoreSystem, CombatSystem, CharacterSystem 등의 실제 코드 예시와 사용 패턴을 포함합니다."
---
# RougeShool 시스템별 핵심 가이드 (System-Specific Guide)

> 목적: 각 시스템별 핵심 사용법과 주의사항을 간단명료하게 제공. 실제 코드 예시 위주로 구성.

## 🏗️ CoreSystem 핵심 사용법

### 자동 초기화 시스템 (ICoreSystemInitializable)
```csharp
// 새로운 시스템 추가 시 필수 구현
public class MySystem : MonoBehaviour, ICoreSystemInitializable
{
    public bool IsInitialized { get; private set; } = false;
    
    public IEnumerator Initialize()
    {
        GameLogger.LogInfo($"{GetType().Name} 초기화 시작", GameLogger.LogCategory.UI);
        
        // 초기화 로직 (비동기 처리)
        yield return InitializeAsync();
        
        IsInitialized = true;
        GameLogger.LogInfo($"{GetType().Name} 초기화 완료", GameLogger.LogCategory.UI);
    }
    
    public void OnInitializationFailed()
    {
        GameLogger.LogError($"{GetType().Name} 초기화 실패", GameLogger.LogCategory.Error);
        IsInitialized = false;
    }
    
    private IEnumerator InitializeAsync()
    {
        // 실제 초기화 로직
        yield return null;
    }
}
```

### 씬 전환 (SceneTransitionManager)
```csharp
// 비동기 씬 전환
await SceneTransitionManager.Instance.TransitionToBattleScene();
await SceneTransitionManager.Instance.TransitionToMainScene();

// 전환 상태 확인
if (SceneTransitionManager.Instance.IsTransitioning)
{
    GameLogger.LogWarning("씬 전환 중입니다", GameLogger.LogCategory.UI);
}
```

### 오디오 시스템 (AudioManager + AudioPoolManager)
```csharp
// BGM 재생 (페이드 인 옵션)
AudioClip bgmClip = Resources.Load<AudioClip>("Sounds/BGM/MainTheme");
AudioManager.Instance.PlayBGM(bgmClip, true); // 페이드 인 활성화

// SFX 재생 (기본 방식)
AudioClip buttonClick = Resources.Load<AudioClip>("Sounds/SFX/ButtonClick");
AudioManager.Instance.PlaySFX(buttonClick);

// SFX 재생 (풀링 사용, 우선순위 지정)
AudioClip cardUse = Resources.Load<AudioClip>("Sounds/SFX/CardUse");
AudioManager.Instance.PlaySFXWithPool(cardUse, 1.0f, 8); // 볼륨, 우선순위

// AudioPoolManager 직접 사용 (고급)
AudioPoolManager poolManager = AudioManager.Instance.GetComponent<AudioPoolManager>();
poolManager.PlaySound(enemyDefeatClip, 1.0f, 10); // 높은 우선순위
```

### 저장 시스템 (SaveManager)
```csharp
// 슬레이 더 스파이어 방식 저장
await SaveManager.Instance.SaveCurrentScene();

// 오디오 설정 저장/로드
SaveManager.Instance.SaveAudioSettings(0.6f, 1.0f);
var (bgm, sfx) = SaveManager.Instance.LoadAudioSettings(0.7f, 1.0f);

// 게임 상태 저장
SaveManager.Instance.SaveGameState("ManualSave");
```

## ⚔️ CombatSystem 핵심 사용법

### 새로운 5슬롯 시스템 (현재 구현)
```csharp
// TurnManager를 통한 턴 관리
TurnManager turnManager = FindObjectOfType<TurnManager>();

// 셋업 페이즈 시작
turnManager.StartSetupPhase();

// 셋업 단계별 카드 배치 (플레이어 우선)
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_4, SlotOwner.Player);
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_3, SlotOwner.Player);
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_4, SlotOwner.Enemy);

// 셋업 완료 후 전투 페이즈로 전환
turnManager.CompleteSetup();

// 현재 페이즈 확인
CombatPhase currentPhase = turnManager.GetCurrentPhase(); // Setup, Battle, End

// 전투 슬롯에서 카드 실행
CombatExecutionManager executor = FindObjectOfType<CombatExecutionManager>();
executor.ExecuteCardInBattleSlot();

// 슬롯 이동 (새로운 시스템)
executor.MoveSlotsForwardNew();

// 턴 완료 (전투 슬롯에서 카드 사용 시에만)
turnManager.CompleteTurn();

// 현재 턴 타입 확인 (새로운 시스템)
TurnType currentTurn = turnManager.GetCurrentTurnTypeNew(); // Player 또는 Enemy
```

### 레거시 4슬롯 시스템 (호환성 유지)
```csharp
// 현재 턴 판정 (1번 슬롯 기준)
var turnType = turnManager.GetCurrentTurnType(); // Player 또는 Enemy

// 플레이어가 1번 슬롯에 카드를 올리는 순간 즉시 실행
CombatExecutionManager executor = FindObjectOfType<CombatExecutionManager>();
executor.ExecuteImmediately(playerCard, CombatSlotPosition.SLOT_1);

// 실행 종료 후 내부적으로 슬롯 이동(2→1, 3→2, 4→3) → 1번이 적 카드라면 즉시 적 실행

// 적 카드 예약(필요 시)
turnManager.RegisterEnemyCardInSlot4(enemyCardDefinition);

// 가드 효과 적용(다음 적 카드 1장 무효화)
turnManager.ApplyGuardEffect();
```

### 상태 패턴 구현
```csharp
// 커스텀 전투 상태 구현
public class CustomCombatState : ICombatTurnState
{
    private TurnManager turnManager;
    
    public CustomCombatState(TurnManager turnManager)
    {
        this.turnManager = turnManager;
    }
    
    public void ExecuteState()
    {
        GameLogger.LogInfo("커스텀 전투 상태 실행", GameLogger.LogCategory.Combat);
        // 상태별 로직 실행
    }
    
    public bool CanTransitionTo(ICombatTurnState nextState)
    {
        // 상태 전환 조건 확인
        return true;
    }
    
    public void OnEnter()
    {
        GameLogger.LogInfo("커스텀 전투 상태 진입", GameLogger.LogCategory.Combat);
    }
    
    public void OnExit()
    {
        GameLogger.LogInfo("커스텀 전투 상태 종료", GameLogger.LogCategory.Combat);
    }
}

// 상태 설정
turnManager.SetState(new CustomCombatState(turnManager));
```

## 👤 CharacterSystem 핵심 사용법

### 플레이어 캐릭터 관리 (PlayerCharacter + PlayerResourceManager)
```csharp
// 캐릭터 생성 및 초기화
PlayerCharacter player = Instantiate(playerPrefab);
player.Initialize(playerData);

// 리소스 관리 (화살/마나)
PlayerResourceManager resourceManager = FindObjectOfType<PlayerResourceManager>();
resourceManager.Initialize(playerData);

// 리소스 소모 가능 여부 확인
if (resourceManager.CanConsumeResource(5))
{
    resourceManager.ConsumeResource(5);
    GameLogger.LogInfo($"리소스 소모: {resourceManager.ResourceName} 5개", GameLogger.LogCategory.Character);
}

// 리소스 회복
resourceManager.RestoreResource(3);

// 리소스 상태 조회
int currentResource = resourceManager.CurrentResource;
int maxResource = resourceManager.MaxResource;
string resourceName = resourceManager.ResourceName;
```

### 새로운 플레이어 UI 시스템 (League of Legends 스타일)
```csharp
// PlayerCharacterUIController 초기화 및 사용
PlayerCharacterUIController uiController = playerCharacter.GetComponent<PlayerCharacterUIController>();

// 캐릭터 초기화 (PlayerCharacter.InitializeCharacter에서 자동 호출됨)
uiController.Initialize(playerCharacter);

// HP 업데이트 (PlayerCharacter.TakeDamage/Heal에서 자동 호출됨)
uiController.UpdateHP(currentHP, maxHP);
uiController.OnTakeDamage(damageAmount);
uiController.OnHeal(healAmount);

// 리소스 업데이트 (타입별 UI 자동 표시)
uiController.UpdateResource(currentResource, maxResource);

// 버프/디버프 아이콘 관리
uiController.AddBuffDebuffIcon(buffIcon, duration, isBuff);
uiController.RemoveBuffDebuffIcon(buffIcon);
uiController.ClearAllBuffDebuffIcons();

// 데이터 기반 문양 설정 (PlayerCharacterData.Emblem 사용)
// 문양이 설정되지 않은 경우 자동으로 기본 문양 로드
```

### 적 캐릭터 관리 (EnemyManager + EnemySpawnerManager)
```csharp
// EnemyManager를 통한 적 캐릭터 관리
EnemyManager enemyManager = FindObjectOfType<EnemyManager>();

// 적 캐릭터 등록
enemyManager.RegisterEnemy(enemy);

// 현재 적 캐릭터 조회
IEnemyCharacter currentEnemy = enemyManager.GetCurrentEnemy();

// 적 캐릭터 등록 해제
enemyManager.UnregisterEnemy();

// 적 캐릭터 초기화
enemyManager.ClearEnemy();

// EnemySpawnerManager를 통한 적 스폰
EnemySpawnerManager spawnerManager = FindObjectOfType<EnemySpawnerManager>();
var spawnResult = spawnerManager.SpawnEnemy(enemyData);
if (spawnResult.IsSuccess)
{
    GameLogger.LogInfo($"적 스폰 성공: {spawnResult.Enemy.CharacterName}", GameLogger.LogCategory.Character);
}
```

## 🃏 SkillCardSystem 핵심 사용법

### 통합 데이터 모델
```csharp
// SkillCardFactory를 통한 통합 카드 생성
SkillCardFactory factory = new SkillCardFactory();
SkillCardDefinition definition = Resources.Load<SkillCardDefinition>("SkillCards/Fireball");

// 플레이어 카드 생성
ISkillCard playerCard = factory.CreateFromDefinition(definition, Owner.Player, "플레이어");

// 적 카드 생성
ISkillCard enemyCard = factory.CreateFromDefinition(definition, Owner.Enemy, "적");
```

### 카드 순환 시스템
```csharp
CardCirculationSystem circulationSystem = FindObjectOfType<CardCirculationSystem>();
circulationSystem.Initialize(initialCards);
List<ISkillCard> drawnCards = circulationSystem.DrawCardsForTurn();
circulationSystem.MoveCardToUsedStorage(usedCard);
```

## 💾 SaveSystem 핵심 사용법

### 슬레이 더 스파이어 방식 저장
```csharp
// AutoSaveManager를 통한 자동 저장
AutoSaveManager autoSaveManager = FindObjectOfType<AutoSaveManager>();

// 특정 조건으로 자동 저장 트리거
autoSaveManager.TriggerAutoSave("TurnCompleted");
autoSaveManager.TriggerAutoSave("EnemyCardPlaced");

// 수동 저장
await autoSaveManager.SaveGameState("ManualSave");
```

### 이벤트 기반 저장
```csharp
SaveEventTrigger saveEventTrigger = FindObjectOfType<SaveEventTrigger>();
saveEventTrigger.OnTurnCompleted();
saveEventTrigger.OnStageCompleted();
```

## 🎵 AudioSystem 핵심 사용법

### 오디오 풀링 고급 사용법
```csharp
AudioPoolManager poolManager = AudioManager.Instance.GetComponent<AudioPoolManager>();

// 우선순위 지정 사운드 재생
AudioClip enemyDefeat = Resources.Load<AudioClip>("Sounds/SFX/EnemyDefeat");
poolManager.PlaySound(enemyDefeat, 1.0f, 10); // 높은 우선순위

// 자동 우선순위 사운드 재생
poolManager.PlaySound(skillActivationClip, 0.8f); // 우선순위 자동 설정
```

## ⚠️ 주의사항 및 금지사항

### AnimationSystem 관련
- **절대 금지**: AnimationSystem 참조 또는 사용
- **현재 상태**: 모든 AnimationSystem 관련 코드 제거 완료
- **임시 처리**: 애니메이션 호출 부분은 Debug.Log로 대체됨

### 레거시 호환성
- **SLOT_1/SLOT_2**: BATTLE_SLOT/WAIT_SLOT_1로 전환 완료
- **CombatSecondAttackState**: 제거 완료, 단일 CombatAttackState로 통합
- **difficultyMultiplier**: StageManager에서 제거 완료

### 성능 최적화
- **DOTween Pro**: 모든 애니메이션은 DOTween Pro 우선 사용
- **Zenject DI**: 의존성 주입으로 느슨한 결합 구현
- **메모리 관리**: 불필요한 참조 해제, 객체 풀링 활용

## 📝 변경 기록
- 2025-01-27 | Maintainer | 시스템별 핵심 가이드 통합 | 문서# RougeShool 시스템별 핵심 가이드 (System-Specific Guide)

> 목적: 각 시스템별 핵심 사용법과 주의사항을 간단명료하게 제공. 실제 코드 예시 위주로 구성.

## 🏗️ CoreSystem 핵심 사용법

### 자동 초기화 시스템 (ICoreSystemInitializable)
```csharp
// 새로운 시스템 추가 시 필수 구현
public class MySystem : MonoBehaviour, ICoreSystemInitializable
{
    public bool IsInitialized { get; private set; } = false;
    
    public IEnumerator Initialize()
    {
        GameLogger.LogInfo($"{GetType().Name} 초기화 시작", GameLogger.LogCategory.UI);
        
        // 초기화 로직 (비동기 처리)
        yield return InitializeAsync();
        
        IsInitialized = true;
        GameLogger.LogInfo($"{GetType().Name} 초기화 완료", GameLogger.LogCategory.UI);
    }
    
    public void OnInitializationFailed()
    {
        GameLogger.LogError($"{GetType().Name} 초기화 실패", GameLogger.LogCategory.Error);
        IsInitialized = false;
    }
    
    private IEnumerator InitializeAsync()
    {
        // 실제 초기화 로직
        yield return null;
    }
}
```

### 씬 전환 (SceneTransitionManager)
```csharp
// 비동기 씬 전환
await SceneTransitionManager.Instance.TransitionToBattleScene();
await SceneTransitionManager.Instance.TransitionToMainScene();

// 전환 상태 확인
if (SceneTransitionManager.Instance.IsTransitioning)
{
    GameLogger.LogWarning("씬 전환 중입니다", GameLogger.LogCategory.UI);
}
```

### 오디오 시스템 (AudioManager + AudioPoolManager)
```csharp
// BGM 재생 (페이드 인 옵션)
AudioClip bgmClip = Resources.Load<AudioClip>("Sounds/BGM/MainTheme");
AudioManager.Instance.PlayBGM(bgmClip, true); // 페이드 인 활성화

// SFX 재생 (기본 방식)
AudioClip buttonClick = Resources.Load<AudioClip>("Sounds/SFX/ButtonClick");
AudioManager.Instance.PlaySFX(buttonClick);

// SFX 재생 (풀링 사용, 우선순위 지정)
AudioClip cardUse = Resources.Load<AudioClip>("Sounds/SFX/CardUse");
AudioManager.Instance.PlaySFXWithPool(cardUse, 1.0f, 8); // 볼륨, 우선순위

// AudioPoolManager 직접 사용 (고급)
AudioPoolManager poolManager = AudioManager.Instance.GetComponent<AudioPoolManager>();
poolManager.PlaySound(enemyDefeatClip, 1.0f, 10); // 높은 우선순위
```

### 저장 시스템 (SaveManager)
```csharp
// 슬레이 더 스파이어 방식 저장
await SaveManager.Instance.SaveCurrentScene();

// 오디오 설정 저장/로드
SaveManager.Instance.SaveAudioSettings(0.6f, 1.0f);
var (bgm, sfx) = SaveManager.Instance.LoadAudioSettings(0.7f, 1.0f);

// 게임 상태 저장
SaveManager.Instance.SaveGameState("ManualSave");
```

## ⚔️ CombatSystem 핵심 사용법

### 새로운 5슬롯 시스템 (현재 구현)
```csharp
// TurnManager를 통한 턴 관리
TurnManager turnManager = FindObjectOfType<TurnManager>();

// 셋업 페이즈 시작
turnManager.StartSetupPhase();

// 셋업 단계별 카드 배치 (플레이어 우선)
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_4, SlotOwner.Player);
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_3, SlotOwner.Player);
turnManager.ProceedSetupStep(CombatSlotPosition.WAIT_SLOT_4, SlotOwner.Enemy);

// 셋업 완료 후 전투 페이즈로 전환
turnManager.CompleteSetup();

// 현재 페이즈 확인
CombatPhase currentPhase = turnManager.GetCurrentPhase(); // Setup, Battle, End

// 전투 슬롯에서 카드 실행
CombatExecutionManager executor = FindObjectOfType<CombatExecutionManager>();
executor.ExecuteCardInBattleSlot();

// 슬롯 이동 (새로운 시스템)
executor.MoveSlotsForwardNew();

// 턴 완료 (전투 슬롯에서 카드 사용 시에만)
turnManager.CompleteTurn();

// 현재 턴 타입 확인 (새로운 시스템)
TurnType currentTurn = turnManager.GetCurrentTurnTypeNew(); // Player 또는 Enemy
```

### 레거시 4슬롯 시스템 (호환성 유지)
```csharp
// 현재 턴 판정 (1번 슬롯 기준)
var turnType = turnManager.GetCurrentTurnType(); // Player 또는 Enemy

// 플레이어가 1번 슬롯에 카드를 올리는 순간 즉시 실행
CombatExecutionManager executor = FindObjectOfType<CombatExecutionManager>();
executor.ExecuteImmediately(playerCard, CombatSlotPosition.SLOT_1);

// 실행 종료 후 내부적으로 슬롯 이동(2→1, 3→2, 4→3) → 1번이 적 카드라면 즉시 적 실행

// 적 카드 예약(필요 시)
turnManager.RegisterEnemyCardInSlot4(enemyCardDefinition);

// 가드 효과 적용(다음 적 카드 1장 무효화)
turnManager.ApplyGuardEffect();
```

### 상태 패턴 구현
```csharp
// 커스텀 전투 상태 구현
public class CustomCombatState : ICombatTurnState
{
    private TurnManager turnManager;
    
    public CustomCombatState(TurnManager turnManager)
    {
        this.turnManager = turnManager;
    }
    
    public void ExecuteState()
    {
        GameLogger.LogInfo("커스텀 전투 상태 실행", GameLogger.LogCategory.Combat);
        // 상태별 로직 실행
    }
    
    public bool CanTransitionTo(ICombatTurnState nextState)
    {
        // 상태 전환 조건 확인
        return true;
    }
    
    public void OnEnter()
    {
        GameLogger.LogInfo("커스텀 전투 상태 진입", GameLogger.LogCategory.Combat);
    }
    
    public void OnExit()
    {
        GameLogger.LogInfo("커스텀 전투 상태 종료", GameLogger.LogCategory.Combat);
    }
}

// 상태 설정
turnManager.SetState(new CustomCombatState(turnManager));
```

## 👤 CharacterSystem 핵심 사용법

### 플레이어 캐릭터 관리 (PlayerCharacter + PlayerResourceManager)
```csharp
// 캐릭터 생성 및 초기화
PlayerCharacter player = Instantiate(playerPrefab);
player.Initialize(playerData);

// 리소스 관리 (화살/마나)
PlayerResourceManager resourceManager = FindObjectOfType<PlayerResourceManager>();
resourceManager.Initialize(playerData);

// 리소스 소모 가능 여부 확인
if (resourceManager.CanConsumeResource(5))
{
    resourceManager.ConsumeResource(5);
    GameLogger.LogInfo($"리소스 소모: {resourceManager.ResourceName} 5개", GameLogger.LogCategory.Character);
}

// 리소스 회복
resourceManager.RestoreResource(3);

// 리소스 상태 조회
int currentResource = resourceManager.CurrentResource;
int maxResource = resourceManager.MaxResource;
string resourceName = resourceManager.ResourceName;
```

### 새로운 플레이어 UI 시스템 (League of Legends 스타일)
```csharp
// PlayerCharacterUIController 초기화 및 사용
PlayerCharacterUIController uiController = playerCharacter.GetComponent<PlayerCharacterUIController>();

// 캐릭터 초기화 (PlayerCharacter.InitializeCharacter에서 자동 호출됨)
uiController.Initialize(playerCharacter);

// HP 업데이트 (PlayerCharacter.TakeDamage/Heal에서 자동 호출됨)
uiController.UpdateHP(currentHP, maxHP);
uiController.OnTakeDamage(damageAmount);
uiController.OnHeal(healAmount);

// 리소스 업데이트 (타입별 UI 자동 표시)
uiController.UpdateResource(currentResource, maxResource);

// 버프/디버프 아이콘 관리
uiController.AddBuffDebuffIcon(buffIcon, duration, isBuff);
uiController.RemoveBuffDebuffIcon(buffIcon);
uiController.ClearAllBuffDebuffIcons();

// 데이터 기반 문양 설정 (PlayerCharacterData.Emblem 사용)
// 문양이 설정되지 않은 경우 자동으로 기본 문양 로드
```

### 적 캐릭터 관리 (EnemyManager + EnemySpawnerManager)
```csharp
// EnemyManager를 통한 적 캐릭터 관리
EnemyManager enemyManager = FindObjectOfType<EnemyManager>();

// 적 캐릭터 등록
enemyManager.RegisterEnemy(enemy);

// 현재 적 캐릭터 조회
IEnemyCharacter currentEnemy = enemyManager.GetCurrentEnemy();

// 적 캐릭터 등록 해제
enemyManager.UnregisterEnemy();

// 적 캐릭터 초기화
enemyManager.ClearEnemy();

// EnemySpawnerManager를 통한 적 스폰
EnemySpawnerManager spawnerManager = FindObjectOfType<EnemySpawnerManager>();
var spawnResult = spawnerManager.SpawnEnemy(enemyData);
if (spawnResult.IsSuccess)
{
    GameLogger.LogInfo($"적 스폰 성공: {spawnResult.Enemy.CharacterName}", GameLogger.LogCategory.Character);
}
```

## 🃏 SkillCardSystem 핵심 사용법

### 통합 데이터 모델
```csharp
// SkillCardFactory를 통한 통합 카드 생성
SkillCardFactory factory = new SkillCardFactory();
SkillCardDefinition definition = Resources.Load<SkillCardDefinition>("SkillCards/Fireball");

// 플레이어 카드 생성
ISkillCard playerCard = factory.CreateFromDefinition(definition, Owner.Player, "플레이어");

// 적 카드 생성
ISkillCard enemyCard = factory.CreateFromDefinition(definition, Owner.Enemy, "적");
```

### 카드 순환 시스템
```csharp
CardCirculationSystem circulationSystem = FindObjectOfType<CardCirculationSystem>();
circulationSystem.Initialize(initialCards);
List<ISkillCard> drawnCards = circulationSystem.DrawCardsForTurn();
circulationSystem.MoveCardToUsedStorage(usedCard);
```

## 💾 SaveSystem 핵심 사용법

### 슬레이 더 스파이어 방식 저장
```csharp
// AutoSaveManager를 통한 자동 저장
AutoSaveManager autoSaveManager = FindObjectOfType<AutoSaveManager>();

// 특정 조건으로 자동 저장 트리거
autoSaveManager.TriggerAutoSave("TurnCompleted");
autoSaveManager.TriggerAutoSave("EnemyCardPlaced");

// 수동 저장
await autoSaveManager.SaveGameState("ManualSave");
```

### 이벤트 기반 저장
```csharp
SaveEventTrigger saveEventTrigger = FindObjectOfType<SaveEventTrigger>();
saveEventTrigger.OnTurnCompleted();
saveEventTrigger.OnStageCompleted();
```

## 🎵 AudioSystem 핵심 사용법

### 오디오 풀링 고급 사용법
```csharp
AudioPoolManager poolManager = AudioManager.Instance.GetComponent<AudioPoolManager>();

// 우선순위 지정 사운드 재생
AudioClip enemyDefeat = Resources.Load<AudioClip>("Sounds/SFX/EnemyDefeat");
poolManager.PlaySound(enemyDefeat, 1.0f, 10); // 높은 우선순위

// 자동 우선순위 사운드 재생
poolManager.PlaySound(skillActivationClip, 0.8f); // 우선순위 자동 설정
```

## ⚠️ 주의사항 및 금지사항

### AnimationSystem 관련
- **절대 금지**: AnimationSystem 참조 또는 사용
- **현재 상태**: 모든 AnimationSystem 관련 코드 제거 완료
- **임시 처리**: 애니메이션 호출 부분은 Debug.Log로 대체됨

### 레거시 호환성
- **SLOT_1/SLOT_2**: BATTLE_SLOT/WAIT_SLOT_1로 전환 완료
- **CombatSecondAttackState**: 제거 완료, 단일 CombatAttackState로 통합
- **difficultyMultiplier**: StageManager에서 제거 완료

### 성능 최적화
- **DOTween Pro**: 모든 애니메이션은 DOTween Pro 우선 사용
- **Zenject DI**: 의존성 주입으로 느슨한 결합 구현
- **메모리 관리**: 불필요한 참조 해제, 객체 풀링 활용

## 📝 변경 기록
- 2025-01-27 | Maintainer | 시스템별 핵심 가이드 통합 | 문서