---
description: "CharacterSystem 개발 가이드 및 규칙 - Unity 2D 캐릭터 통합 관리"
globs: ["Assets/Script/CharacterSystem/**/*.cs"]
rule_id: character_system_rules
version: 1.2.0
alwaysApply: true
---

# CharacterSystem 개발 규칙

## 📋 시스템 개요
CharacterSystem은 게임의 모든 캐릭터(플레이어, 적)를 관리하는 시스템입니다. 캐릭터의 기본 속성, 상태, 행동을 통합적으로 관리합니다.

## 🏗️ 폴더 구조 규칙
```
CharacterSystem/
├── Core/             # 캐릭터 핵심 로직 (4개 파일)
├── Data/             # 캐릭터 데이터 (4개 파일)
├── Interface/        # 캐릭터 인터페이스 (8개 파일)
├── Manager/          # 캐릭터 매니저 (3개 파일)
├── Initialization/   # 캐릭터 초기화 (6개 파일)
├── Slot/             # 캐릭터 슬롯 (1개 파일)
├── UI/               # 캐릭터 UI (2개 파일)
└── Utility/          # 캐릭터 유틸리티 (4개 파일)
```

## 📁 주요 컴포넌트 규칙

### Core 폴더 규칙 (4개 파일)
- **CharacterBase.cs**: 모든 캐릭터의 기본 클래스 (MonoBehaviour 상속)
- **PlayerCharacter.cs**: 플레이어 캐릭터 구현
- **EnemyCharacter.cs**: 적 캐릭터 구현
- **CharacterState.cs**: 캐릭터 상태 관리

### Data 폴더 규칙 (4개 파일)
- **PlayerCharacterData.cs**: 플레이어 캐릭터 데이터 (ScriptableObject)
- **EnemyCharacterData.cs**: 적 캐릭터 데이터 (ScriptableObject)
- **PlayerCharacterType.cs**: 플레이어 캐릭터 타입 열거형
- **PlayerCharacterTypeHelper.cs**: 플레이어 캐릭터 타입 헬퍼

### Interface 폴더 규칙 (8개 파일)
- **ICharacter.cs**: 캐릭터 기본 인터페이스
- **ICharacterData.cs**: 캐릭터 데이터 인터페이스
- **ICharacterState.cs**: 캐릭터 상태 인터페이스
- **ICharacterAction.cs**: 캐릭터 행동 인터페이스
- **ICharacterEffect.cs**: 캐릭터 효과 인터페이스
- **ICharacterUI.cs**: 캐릭터 UI 인터페이스
- **IPlayerResourceManager.cs**: 플레이어 리소스 관리 인터페이스
- **ICharacterDeathListener.cs**: 캐릭터 사망 리스너 인터페이스

### Manager 폴더 규칙 (3개 파일)
- **PlayerManager.cs**: 플레이어 캐릭터 매니저 (싱글톤)
- **EnemyManager.cs**: 적 캐릭터 매니저 (싱글톤)
- **PlayerResourceManager.cs**: 플레이어 리소스 관리 매니저

### Initialization 폴더 규칙 (6개 파일)
- **EnemyCharacterInitializer.cs**: 적 캐릭터 초기화
- **EnemyHandInitializer.cs**: 적 핸드 초기화
- **EnemyInitializer.cs**: 적 초기화 통합 관리
- **HandInitializer.cs**: 핸드 초기화 기본 클래스
- **PlayerCharacterInitializer.cs**: 플레이어 캐릭터 초기화
- **PlayerSkillCardInitializer.cs**: 플레이어 스킬카드 초기화

### UI 폴더 규칙 (2개 파일)
- **CharacterSlotUI.cs**: 캐릭터 슬롯 UI
- **CharacterUIController.cs**: 캐릭터 UI 컨트롤러

### Utility 폴더 규칙 (4개 파일)
- **CharacterDeathHandler.cs**: 캐릭터 사망 처리
- **CardRegistrar.cs**: 카드 등록기
- **CardValidator.cs**: 카드 검증기
- **EnemySpawnResult.cs**: 적 스폰 결과

## 🎯 주요 기능

1. **캐릭터 타입별 특수 기능**: 검, 활, 지팡이 캐릭터의 고유한 특성과 능력
2. **리소스 관리 시스템**: 화살(활), 마나(지팡이) 등 캐릭터별 리소스 관리
3. **상태 관리**: 생존, 가드, 효과 상태 등 캐릭터 상태 통합 관리
4. **초기화 시스템**: Zenject DI를 통한 자동 초기화 및 스킬카드 설정
5. **매니저 패턴**: 플레이어/적 캐릭터 통합 관리
6. **이벤트 기반 통신**: 캐릭터 사망, 상태 변경 등 이벤트 시스템
7. **데이터 기반 설계**: ScriptableObject를 통한 캐릭터 데이터 관리
8. **UI 연동**: 캐릭터 슬롯 UI 및 컨트롤러 지원

## 🎯 개발 규칙

### 1. 캐릭터 기본 속성 규칙
- **체력 (Health)**: 캐릭터의 생명력 (currentHP, maxHP)
- **가드 (Guard)**: 데미지 감소 방어력 (currentGuard, isGuarded)
- **리소스 (Resource)**: 캐릭터 타입별 리소스 (Bow: 화살, Staff: 마나, Sword: 0)
- **턴 효과 (PerTurnEffect)**: 턴마다 적용되는 효과들

### 2. 플레이어 캐릭터 타입 규칙
- **검 (Sword)**: 근접 전투 특화
- **활 (Bow)**: 원거리 전투 특화, 화살 리소스 관리
- **지팡이 (Staff)**: 마법 전투 특화, 마나 리소스 관리

### 3. 상태 관리 규칙
- **생존 상태**: 살아있음/죽음 (bool isAlive)
- **효과 상태**: 버프/디버프 효과 (List<Effect>)
- **상태 전환**: 이벤트 기반 상태 변경

### 4. 행동 시스템 규칙
- **기본 공격**: 일반적인 공격 행동
- **스킬 사용**: 특수 능력 사용
- **행동 검증**: 행동 가능 여부 사전 검증

### 5. 리소스 관리 규칙
- **화살 (Arrows)**: 활 캐릭터 전용 리소스
- **마나 (Mana)**: 지팡이 캐릭터 전용 리소스
- **리소스 소모**: 스킬 사용 시 리소스 소모
- **리소스 복구**: 턴 종료 시 리소스 복구

### 6. 초기화 시스템 규칙
- **자동 초기화**: 캐릭터 생성 시 자동 설정
- **스킬카드 초기화**: 캐릭터별 스킬카드 덱 설정
- **Zenject DI**: 의존성 주입을 통한 초기화

## 🔧 주요 클래스 및 메서드

### EnemyManager 클래스
- **Instance**: 싱글톤 인스턴스 (프로퍼티)
- **RegisterEnemy()**: 적 캐릭터 등록
- **UnregisterEnemy()**: 적 캐릭터 등록 해제
- **GetCurrentEnemy()**: 현재 적 캐릭터 조회
- **ClearEnemy()**: 적 캐릭터 초기화
- **IsEnemyRegistered**: 적 등록 상태 (프로퍼티)

### PlayerResourceManager 클래스
- **Initialize()**: 리소스 매니저 초기화
- **CanConsumeResource()**: 리소스 소모 가능 여부 확인
- **ConsumeResource()**: 리소스 소모
- **RestoreResource()**: 리소스 회복
- **CurrentResource**: 현재 리소스 (프로퍼티)
- **MaxResource**: 최대 리소스 (프로퍼티)
- **ResourceName**: 리소스 이름 (프로퍼티)

### EnemySpawnerManager 클래스
- **SpawnEnemy()**: 적 캐릭터 스폰
- **GetSpawnPosition()**: 스폰 위치 조회
- **IsSpawnAvailable()**: 스폰 가능 여부 확인

### ICharacterSlot 인터페이스
- **SetCharacter()**: 캐릭터 설정
- **GetCharacter()**: 캐릭터 조회
- **IsEmpty**: 빈 슬롯 여부 (프로퍼티)
- **ClearSlot()**: 슬롯 초기화

### ICharacterSlotRegistry 인터페이스
- **RegisterSlot()**: 슬롯 등록
- **UnregisterSlot()**: 슬롯 등록 해제
- **GetSlot()**: 슬롯 조회
- **GetAllSlots()**: 모든 슬롯 조회

### PlayerCharacterData 클래스
- **CharacterType**: 캐릭터 타입 (프로퍼티)
- **MaxHealth**: 최대 체력 (프로퍼티)
- **MaxResource**: 최대 리소스 (프로퍼티)
- **ResourceType**: 리소스 타입 (프로퍼티)
- **SkillCards**: 스킬카드 목록 (프로퍼티)

### EnemyCharacter 클래스
- **Initialize()**: 적 캐릭터 초기화
- **TakeDamage()**: 데미지 받기
- **Die()**: 사망 처리
- **IsAlive**: 생존 상태 (프로퍼티)
- **CurrentHealth**: 현재 체력 (프로퍼티)

## 🔧 사용 방법

### 기본 사용법
```csharp
// 캐릭터 생성 및 초기화
PlayerCharacter player = Instantiate(playerPrefab);
player.Initialize(playerData);

EnemyCharacter enemy = Instantiate(enemyPrefab);
enemy.Initialize(enemyData);

// 캐릭터 상태 확인
if (player.IsAlive)
{
    player.Attack(enemy);
}
```

### 매니저를 통한 캐릭터 관리
```csharp
// EnemyManager를 통한 적 캐릭터 관리
EnemyManager enemyManager = FindObjectOfType<EnemyManager>();

// 적 캐릭터 등록
enemyManager.RegisterEnemy(enemy);

// 현재 적 캐릭터 조회
IEnemyCharacter currentEnemy = enemyManager.GetCurrentEnemy();

// 적 캐릭터 등록 해제
enemyManager.UnregisterEnemy();

// 적 캐릭터 초기화
enemyManager.ClearEnemy();
```

### 리소스 관리
```csharp
// PlayerResourceManager를 통한 리소스 관리
PlayerResourceManager resourceManager = FindObjectOfType<PlayerResourceManager>();

// 캐릭터 데이터로 초기화
resourceManager.Initialize(playerData);

// 리소스 소모 가능 여부 확인
if (resourceManager.CanConsumeResource(5))
{
    resourceManager.ConsumeResource(5);
}

// 리소스 회복
resourceManager.RestoreResource(3);

// 리소스 상태 조회
int currentResource = resourceManager.CurrentResource;
int maxResource = resourceManager.MaxResource;
string resourceName = resourceManager.ResourceName;
```

### 캐릭터 타입별 특수 기능
```csharp
// 플레이어 캐릭터 타입별 특수 기능
if (player.CharacterType == PlayerCharacterType.Sword)
{
    // 검 캐릭터 특수 기능
    player.SwordAttack();
}
else if (player.CharacterType == PlayerCharacterType.Bow)
{
    // 활 캐릭터 특수 기능 (화살 리소스 사용)
    if (resourceManager.CanConsumeResource(1))
    {
        resourceManager.ConsumeResource(1);
        player.BowAttack();
    }
}
else if (player.CharacterType == PlayerCharacterType.Staff)
{
    // 지팡이 캐릭터 특수 기능 (마나 리소스 사용)
    if (resourceManager.CanConsumeResource(2))
    {
        resourceManager.ConsumeResource(2);
        player.StaffAttack();
    }
}
```

## 🏗️ 아키텍처 패턴

### 1. 상속 구조
- **CharacterBase**: 모든 캐릭터의 기본 클래스
- **PlayerCharacter**: 플레이어 캐릭터 구현
- **EnemyCharacter**: 적 캐릭터 구현

### 2. 매니저 패턴
- **PlayerManager**: 플레이어 캐릭터 통합 관리
- **EnemyManager**: 적 캐릭터 통합 관리
- **PlayerResourceManager**: 플레이어 리소스 관리

### 3. 초기화 패턴
- **Initializer 클래스들**: 캐릭터별 초기화 로직
- **Zenject DI**: 의존성 주입을 통한 초기화

### 4. 이벤트 패턴
- **ICharacterDeathListener**: 캐릭터 사망 이벤트
- **상태 변경 이벤트**: 캐릭터 상태 변경 알림

## 🔧 기술적 구현 세부사항

### 성능 최적화
- **객체 풀링**: 캐릭터 생성/소멸 최적화
- **이벤트 기반**: Update() 최소화
- **메모리 관리**: 불필요한 참조 해제

### 스레드 안전성
- **싱글톤 매니저**: 스레드 안전한 매니저 구현
- **상태 동기화**: 캐릭터 상태 변경 동기화

### 메모리 관리
- **리소스 해제**: 캐릭터 소멸 시 리소스 정리
- **이벤트 구독 해제**: 메모리 누수 방지

## 🏗️ 시스템 아키텍처

### 의존성 다이어그램
```mermaid
graph TD
    A[PlayerManager] --> B[PlayerCharacter]
    A --> C[PlayerResourceManager]
    D[EnemyManager] --> E[EnemyCharacter]
    D --> F[EnemySpawnerManager]
    G[CharacterSlotUI] --> H[ICharacterSlot]
    I[CharacterUIController] --> J[CharacterSlotUI]
    K[PlayerCharacterInitializer] --> L[PlayerCharacterData]
    M[EnemyCharacterInitializer] --> N[EnemyCharacterData]
    O[CardRegistrar] --> P[SkillCardSystem]
    Q[CardValidator] --> R[CombatSystem]
```

### 클래스 다이어그램
```mermaid
classDiagram
    class CharacterBase {
        +Initialize()
        +TakeDamage()
        +Die()
        +IsAlive: bool
        +CurrentHealth: int
    }
    
    class PlayerCharacter {
        +CharacterType: PlayerCharacterType
        +SwordAttack()
        +BowAttack()
        +StaffAttack()
    }
    
    class EnemyCharacter {
        +Initialize()
        +TakeDamage()
        +Die()
    }
    
    class PlayerManager {
        +Instance: PlayerManager
        +GetPlayerCharacter()
        +SetPlayerCharacter()
    }
    
    class EnemyManager {
        +Instance: EnemyManager
        +RegisterEnemy()
        +GetCurrentEnemy()
        +ClearEnemy()
    }
    
    class PlayerResourceManager {
        +Initialize()
        +CanConsumeResource()
        +ConsumeResource()
        +RestoreResource()
    }
    
    CharacterBase <|-- PlayerCharacter
    CharacterBase <|-- EnemyCharacter
    PlayerManager --> PlayerCharacter
    EnemyManager --> EnemyCharacter
    PlayerManager --> PlayerResourceManager
```

### 시퀀스 다이어그램
```mermaid
sequenceDiagram
    participant Client
    participant PlayerManager
    participant PlayerCharacter
    participant PlayerResourceManager
    participant SkillCardSystem
    
    Client->>PlayerManager: SetPlayerCharacter(data)
    PlayerManager->>PlayerCharacter: Initialize(data)
    PlayerCharacter->>PlayerResourceManager: Initialize(data)
    PlayerManager->>SkillCardSystem: RegisterSkillCards()
    SkillCardSystem-->>PlayerManager: Cards Registered
    PlayerManager-->>Client: Character Ready
```

## 🧪 검증/테스트 지침

### 체크리스트
- [ ] 캐릭터 타입별 특수 기능 구현 확인
- [ ] 리소스 관리 로직 검증
- [ ] 상태 전환 이벤트 정상 동작 확인
- [ ] 초기화 순서 및 의존성 확인
- [ ] 메모리 누수 검사

### 재현 절차
1. 각 캐릭터 타입별 생성/초기화 테스트
2. 리소스 소모/복구 로직 테스트
3. 상태 전환 시나리오 테스트
4. 성능 프로파일링

## 📚 참고 자료

### 관련 문서
- [CoreSystem 개발 문서](../CoreSystem/CoreSystem_개발문서.md)
- [CombatSystem 개발 문서](../CombatSystem/CombatSystem_개발문서.md)
- [SkillCardSystem 개발 문서](../SkillCardSystem/SkillCardSystem_개발문서.md)
- [AnimationSystem 개발 문서](../AnimationSystem/AnimationSystem_개발문서.md)

### Unity 문서
- [MonoBehaviour](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html)
- [ScriptableObject](https://docs.unity3d.com/Manual/class-ScriptableObject.html)
- [Unity Events](https://docs.unity3d.com/Manual/UnityEvents.html)

### 디자인 패턴
- [Manager Pattern](https://refactoring.guru/design-patterns/singleton)
- [Strategy Pattern](https://refactoring.guru/design-patterns/strategy)
- [Observer Pattern](https://refactoring.guru/design-patterns/observer)
- [Factory Pattern](https://refactoring.guru/design-patterns/factory-method)

## 📝 변경 기록(Delta)
- 형식: `YYYY-MM-DD | 작성자 | 변경 요약 | 영향도(코드/씬/문서)`

- 2025-01-27 | Maintainer | CharacterSystem 개발 문서 초기 작성 | 문서
- 2025-01-27 | Maintainer | 실제 폴더 구조 반영 및 Initialization 폴더명 수정 | 문서
- 2025-01-27 | Maintainer | .mdc 규칙 파일로 변환 및 Cursor 규칙 저장 | 문서
- 2025-01-27 | Maintainer | 실제 코드 분석 기반 구체적 클래스/메서드/인터페이스 정보 추가 | 문서
- 2025-01-27 | Maintainer | 실제 코드 기반 캐릭터 기본 속성 수정 (속도/공격력 제거, 가드/리소스/턴효과 추가) | 문서
- 2025-01-27 | Maintainer | 개발 문서 기반 규칙 파일 업데이트 (주요 기능, 클래스/메서드, 아키텍처 다이어그램 추가) | 문서