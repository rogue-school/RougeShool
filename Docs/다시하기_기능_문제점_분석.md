# "다시하기" 기능 문제점 분석 보고서

## 📋 개요
`SettingsPanelController.OnResetProgressClicked()` 메서드의 "다시하기" 기능에서 발생할 수 있는 문제점들을 분석한 보고서입니다.

## 🔴 발견된 주요 문제점

### 1. **타이밍 문제: 플레이어 재생성과 스테이지 시작의 Race Condition**

**위치**: `SettingsPanelController.cs:417-583`

**문제점**:
```csharp
// 4. 플레이어 재생성 (비동기)
playerManager.CreateAndRegisterCharacter(); // 즉시 반환, 실제 생성은 비동기

// 6. 코루틴에서 대기 후 스테이지 시작
StartCoroutine(ResetProgressCoroutine());
```

**상세 분석**:
- `CreateAndRegisterCharacter()`는 입장 애니메이션(`TryPlayEntranceAnimation`)을 포함하여 **비동기로 실행**됩니다
- `OnPlayerCharacterReady` 이벤트는 **입장 애니메이션 완료 후** 발생합니다 (약 1.5초 소요)
- 하지만 `ResetProgressCoroutine()`은 최대 2초(20회 × 0.1초)만 대기합니다
- **플레이어가 완전히 준비되기 전에 스테이지가 시작될 수 있습니다**

**예상 증상**:
- 플레이어 UI가 연결되지 않은 상태로 전투 시작
- 핸드 매니저가 초기화되지 않은 상태
- 플레이어 리소스가 제대로 표시되지 않음

---

### 2. **중복 초기화: 인벤토리 초기화가 두 번 실행됨**

**위치**: `SettingsPanelController.cs:460-468`, `StageManager.cs:200-215`

**문제점**:
```csharp
// SettingsPanelController에서
itemService.ResetInventoryForNewGame(); // 1차 초기화

// PlayerPrefs 플래그 설정
PlayerPrefs.SetInt("NEW_GAME_REQUESTED", 1);

// StageManager.Start()에서
if (PlayerPrefs.GetInt("NEW_GAME_REQUESTED", 0) == 1)
{
    InitializeGameStateForNewGame(); // 2차 초기화 (중복!)
    itemService.ResetInventoryForNewGame(); // 또 다시 초기화
}
```

**예상 증상**:
- 불필요한 성능 오버헤드
- 초기화 중 발생하는 이벤트가 중복 발생할 수 있음

---

### 3. **전투 상태 머신 리셋 누락**

**위치**: `SettingsPanelController.cs:556-566`

**문제점**:
```csharp
// 스테이지 1 로드
if (concreteStageManager.LoadStage(1))
{
    // 스테이지 시작 (전투 상태 머신 리셋 없이 바로 시작)
    concreteStageManager.StartStage();
}
```

**상세 분석**:
- `StartStage()` 내부에서 `ResetCombatState()`를 호출하지만, **이미 전투가 진행 중인 상태**에서 호출될 경우 문제가 발생할 수 있습니다
- `ResetProgressCoroutine()`에서 `StartStage()`를 호출하기 전에 **명시적으로 전투 상태를 리셋하지 않습니다**

**예상 증상**:
- 이전 전투 상태가 남아있어 새로운 전투 시작 시 충돌
- 코루틴이 정리되지 않아 메모리 누수 가능성

---

### 4. **플레이어 GameObject 파괴 타이밍 문제**

**위치**: `SettingsPanelController.cs:483-492`

**문제점**:
```csharp
// 플레이어 GameObject 제거
if (currentPlayer is MonoBehaviour playerMono)
{
    UnityEngine.Object.Destroy(playerMono.gameObject); // 즉시 파괴 아님 (다음 프레임)
}

// 플레이어 등록 해제
playerManager.UnregisterCharacter(); // currentCharacter = null

// 플레이어 다시 생성 (이전 GameObject가 아직 파괴되지 않았을 수 있음)
playerManager.CreateAndRegisterCharacter();
```

**상세 분석**:
- `Destroy()`는 즉시 파괴하지 않고 **다음 프레임에 파괴**됩니다
- `CreateAndRegisterCharacter()`가 호출될 때 이전 GameObject가 아직 존재할 수 있습니다
- `PlayerManager.CreateAndRegisterCharacter()` 내부에서 `currentCharacter != null` 체크를 하지만, **GameObject는 파괴 중인 상태**일 수 있습니다

**예상 증상**:
- 중복 플레이어 GameObject 생성 가능성
- 이벤트 구독이 해제되지 않은 상태에서 새 플레이어 생성

---

### 5. **이벤트 구독 해제 누락**

**위치**: `SettingsPanelController.cs:445-457`, `PlayerCharacterUIController.cs:263-265`

**문제점**:
- 플레이어를 파괴하기 전에 `PlayerCharacterUIController`의 이벤트 구독이 해제되지 않습니다
- `PlayerCharacter.OnDestroy()`에서 이벤트를 null로 설정하지만, **UI 컨트롤러는 여전히 구독 중**일 수 있습니다

**예상 증상**:
- 파괴된 플레이어에 대한 이벤트 호출로 인한 NullReferenceException
- 메모리 누수

---

### 6. **슬롯 정리 누락**

**위치**: `SettingsPanelController.cs:434-443`

**문제점**:
```csharp
// 1. 플레이어 핸드 초기화
playerHandManager.ClearAll(); // 핸드만 정리

// 하지만 전투 슬롯은 정리하지 않음!
// StageManager.StartStage()에서 ClearEnemyCardsFromSlots()를 호출하지만,
// 플레이어 카드 슬롯은 정리되지 않을 수 있음
```

**상세 분석**:
- `TurnManager.ClearAllSlots()`가 호출되지 않습니다
- `SlotMovementController.ResetSlotStates()`가 호출되지 않습니다
- 이전 전투의 카드가 슬롯에 남아있을 수 있습니다

**예상 증상**:
- 이전 전투의 카드가 슬롯에 남아있음
- 슬롯 상태가 초기화되지 않아 새로운 전투 시작 시 문제 발생

---

### 7. **StageManager의 isWaitingForPlayer 플래그 미처리**

**위치**: `SettingsPanelController.cs:556-566`, `StageManager.cs:272-276`

**문제점**:
```csharp
// SettingsPanelController에서
concreteStageManager.StartStage(); // 직접 호출

// 하지만 StageManager는 플레이어 준비 완료를 기다리는 로직이 있음
private void OnPlayerReady(ICharacter player)
{
    if (isWaitingForPlayer)
    {
        isWaitingForPlayer = false;
        StartStage(); // 여기서 StartStage()가 호출됨
    }
}
```

**상세 분석**:
- `ResetProgressCoroutine()`에서 `StartStage()`를 직접 호출하지만, `OnPlayerCharacterReady` 이벤트도 발생합니다
- `isWaitingForPlayer` 플래그가 설정되지 않아 **이벤트 핸들러에서 StartStage()가 호출되지 않지만**, 직접 호출과 이벤트 기반 호출이 **중복될 가능성**이 있습니다

**예상 증상**:
- `StartStage()`가 두 번 호출될 수 있음
- 적이 중복 생성될 수 있음

---

### 8. **플레이어 재생성 실패 시 복구 로직 부족**

**위치**: `SettingsPanelController.cs:543-549`

**문제점**:
```csharp
if (playerManager.GetCharacter() == null)
{
    // 재시도
    playerManager.CreateAndRegisterCharacter();
    yield return new WaitForSeconds(0.5f);
    // 하지만 재시도 후에도 실패하면 그냥 진행함
}
```

**예상 증상**:
- 플레이어가 생성되지 않은 상태로 전투 시작
- NullReferenceException 발생 가능성

---

## 🟡 중간 심각도 문제점

### 9. **DOTween 애니메이션 정리 누락**

**위치**: `SettingsPanelController.cs:483-488`

**문제점**:
- 플레이어 GameObject를 파괴하기 전에 DOTween 애니메이션을 명시적으로 정리하지 않습니다
- `CharacterBase.OnDestroy()`에서 정리하지만, **파괴 전에 정리하는 것이 더 안전**합니다

---

### 10. **에러 핸들링 부족**

**위치**: `SettingsPanelController.cs:517-522`

**문제점**:
- `ResetProgressCoroutine()`에서 예외가 발생하면 **설정창이 닫히지 않을 수 있습니다**
- 사용자가 게임을 계속할 수 없는 상태가 될 수 있습니다

---

## 🟢 낮은 심각도 문제점

### 11. **로깅 일관성 부족**

**위치**: 전체 코드

**문제점**:
- 일부는 `GameLogger.LogInfo`, 일부는 `GameLogger.LogWarning`을 사용
- 로그 레벨이 일관되지 않아 디버깅이 어려울 수 있습니다

---

## 📊 문제점 우선순위 요약

| 우선순위 | 문제점 | 예상 영향 | 해결 난이도 |
|---------|--------|----------|------------|
| 🔴 **P0** | 타이밍 문제 (Race Condition) | 게임 진행 불가 | 높음 |
| 🔴 **P0** | 전투 상태 머신 리셋 누락 | 게임 크래시 가능 | 중간 |
| 🔴 **P1** | 슬롯 정리 누락 | 게임 상태 불일치 | 중간 |
| 🟡 **P2** | 중복 초기화 | 성능 저하 | 낮음 |
| 🟡 **P2** | GameObject 파괴 타이밍 | 간헐적 버그 | 중간 |
| 🟡 **P2** | 이벤트 구독 해제 누락 | 메모리 누수 | 중간 |
| 🟡 **P3** | 에러 핸들링 부족 | UX 저하 | 낮음 |

---

## 💡 권장 해결 방안

### 1. 플레이어 재생성 완료 대기 개선
```csharp
// OnPlayerCharacterReady 이벤트를 기다리도록 수정
private System.Collections.IEnumerator ResetProgressCoroutine()
{
    bool playerReady = false;
    System.Action<ICharacter> onReady = (player) => { playerReady = true; };
    
    if (playerManager != null)
    {
        playerManager.OnPlayerCharacterReady += onReady;
        
        // 플레이어 재생성
        playerManager.CreateAndRegisterCharacter();
        
        // 이벤트 발생 대기 (최대 5초)
        float timeout = 5f;
        float elapsed = 0f;
        while (!playerReady && elapsed < timeout)
        {
            yield return new WaitForSeconds(0.1f);
            elapsed += 0.1f;
        }
        
        playerManager.OnPlayerCharacterReady -= onReady;
        
        if (!playerReady)
        {
            GameLogger.LogError("플레이어 재생성 실패 - 게임을 계속할 수 없습니다", GameLogger.LogCategory.Error);
            yield break;
        }
    }
    
    // 나머지 로직...
}
```

### 2. 전투 상태 머신 명시적 리셋
```csharp
// StartStage() 호출 전에 명시적으로 리셋
var combatStateMachine = FindFirstObjectByType<CombatStateMachine>();
if (combatStateMachine != null)
{
    combatStateMachine.ResetCombatState();
    yield return new WaitForSeconds(0.2f); // 리셋 완료 대기
}
```

### 3. 슬롯 정리 추가
```csharp
// TurnManager와 SlotMovementController 정리
var turnManager = FindFirstObjectByType<TurnManager>();
if (turnManager != null)
{
    turnManager.ClearAllSlots();
    turnManager.ClearEnemyCache();
    turnManager.ResetSlotStates();
}
```

### 4. GameObject 파괴 대기
```csharp
// Destroy 후 다음 프레임까지 대기
if (currentPlayer is MonoBehaviour playerMono)
{
    UnityEngine.Object.Destroy(playerMono.gameObject);
    yield return new WaitForEndOfFrame(); // 파괴 완료 대기
    yield return null;
}
```

---

## 📝 결론

"다시하기" 기능은 **여러 시스템 간의 복잡한 상호작용**을 포함하고 있어, **타이밍 문제와 상태 동기화 문제**가 발생하기 쉽습니다. 특히 **비동기 작업(플레이어 재생성, 애니메이션)**과 **동기 작업(스테이지 시작)**이 섞여 있어 Race Condition이 발생할 수 있습니다.

**가장 중요한 문제**는 **플레이어 재생성 완료를 제대로 기다리지 않고 스테이지를 시작**하는 것입니다. 이를 해결하기 위해서는 **이벤트 기반 대기** 또는 **더 긴 대기 시간**이 필요합니다.

