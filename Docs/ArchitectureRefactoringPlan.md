# RougeShool 아키텍처 리팩토링 계획

> 작성일: 2025-11-24  
> 목적: 시스템별 분리 방식에서 더 깔끔하고 확장 가능한 아키텍처로 완전 재구성

---

## 📊 현재 구조 분석

### 현재 구조의 문제점

1. **시스템 간 강한 결합**
   - CombatSystem → CharacterSystem → SkillCardSystem 순환 의존성
   - 시스템 경계가 모호함
   - 인터페이스 분리 부족

2. **폴더 구조의 중복**
   - 각 시스템마다 Core, Data, Interface, Manager, UI 반복
   - 유사한 역할의 클래스가 여러 시스템에 분산
   - 공통 기능의 재사용 어려움

3. **책임 분리 부족**
   - Manager 클래스가 너무 많은 책임을 가짐
   - 비즈니스 로직과 인프라 코드 혼재
   - UI 로직과 도메인 로직 분리 부족

4. **확장성 문제**
   - 새 기능 추가 시 여러 시스템 수정 필요
   - 테스트 어려움 (의존성 복잡)
   - 코드 재사용 어려움

---

## 🏗️ 제안하는 아키텍처 패턴

### 옵션 1: 레이어드 아키텍처 (Layered Architecture) ⭐ **추천**

**구조**:
```
Script/
├── Domain/              # 도메인 모델 (비즈니스 로직)
│   ├── Character/      # 캐릭터 도메인
│   ├── Combat/         # 전투 도메인
│   ├── Card/           # 카드 도메인
│   ├── Item/           # 아이템 도메인
│   └── Stage/          # 스테이지 도메인
├── Application/        # 애플리케이션 서비스 (유스케이스)
│   ├── Services/       # 비즈니스 로직 서비스
│   ├── Commands/       # 명령 (CQRS 패턴)
│   ├── Queries/        # 쿼리 (CQRS 패턴)
│   └── Events/        # 도메인 이벤트
├── Infrastructure/     # 인프라스트럭처
│   ├── Unity/          # Unity 특화 코드
│   ├── Zenject/        # DI 바인딩
│   ├── Persistence/    # 저장/로드
│   └── Audio/          # 오디오 시스템
└── Presentation/       # 프레젠테이션 레이어
    ├── UI/             # UI 컨트롤러
    ├── VFX/            # VFX 시스템
    └── Input/           # 입력 처리
```

**장점**:
- ✅ 명확한 책임 분리
- ✅ 테스트 용이성
- ✅ 의존성 방향 명확 (Domain ← Application ← Infrastructure ← Presentation)
- ✅ 비즈니스 로직 재사용 가능

**단점**:
- ⚠️ 초기 구조 변경 작업량 많음
- ⚠️ Unity 특성상 일부 레이어 경계 모호할 수 있음

---

### 옵션 2: Clean Architecture (Onion Architecture)

**구조**:
```
Script/
├── Core/               # 핵심 비즈니스 로직 (의존성 없음)
│   ├── Entities/      # 엔티티 (Character, Card, Item)
│   ├── UseCases/      # 유스케이스 (StartCombat, PlayCard)
│   └── Interfaces/    # 인터페이스 정의
├── Adapters/          # 어댑터 (외부 시스템 연결)
│   ├── Input/         # 입력 어댑터
│   ├── Output/        # 출력 어댑터 (UI, VFX)
│   └── Persistence/   # 저장 어댑터
└── Frameworks/        # 프레임워크 (Unity, Zenject)
    ├── Unity/         # Unity 특화
    └── DI/            # DI 바인딩
```

**장점**:
- ✅ 의존성 역전 원칙 완벽 적용
- ✅ 도메인 로직 완전 독립
- ✅ 테스트 용이성 최고

**단점**:
- ⚠️ Unity 게임에 과도할 수 있음
- ⚠️ 복잡도 증가
- ⚠️ 초기 설정 작업량 많음

---

### 옵션 3: Feature-Based 구조 (Feature Slicing)

**구조**:
```
Script/
├── Features/          # 기능별 분리
│   ├── Battle/        # 전투 기능 (모든 것 포함)
│   │   ├── Domain/    # 도메인 로직
│   │   ├── Application/ # 애플리케이션 로직
│   │   ├── Infrastructure/ # 인프라
│   │   └── Presentation/ # UI
│   ├── Character/     # 캐릭터 기능
│   ├── Card/          # 카드 기능
│   └── Item/          # 아이템 기능
├── Shared/           # 공통 기능
│   ├── Core/         # 핵심 유틸리티
│   ├── Audio/        # 오디오
│   └── Save/         # 저장
└── Common/           # 공통 컴포넌트
    ├── UI/           # 공통 UI
    └── VFX/          # 공통 VFX
```

**장점**:
- ✅ 기능별로 완전히 독립
- ✅ 새 기능 추가 용이
- ✅ 팀 작업 시 충돌 최소화

**단점**:
- ⚠️ 공통 코드 중복 가능성
- ⚠️ 기능 간 통신 복잡

---

### 옵션 4: Hybrid 구조 (레이어 + 기능) ⭐⭐ **최고 추천**

**구조**:
```
Script/
├── Domain/           # 도메인 레이어 (비즈니스 로직)
│   ├── Character/   # 캐릭터 도메인
│   │   ├── Entities/ # Character, PlayerCharacter, EnemyCharacter
│   │   ├── ValueObjects/ # CharacterStats, Resource
│   │   └── Interfaces/ # ICharacter, IPlayerCharacter
│   ├── Combat/      # 전투 도메인
│   │   ├── Entities/ # CombatSlot, Turn
│   │   ├── ValueObjects/ # CombatPhase, SlotPosition
│   │   └── Interfaces/ # ITurnManager, ICombatExecutor
│   ├── Card/        # 카드 도메인
│   │   ├── Entities/ # SkillCard, CardEffect
│   │   ├── ValueObjects/ # CardDefinition, CardStats
│   │   └── Interfaces/ # ISkillCard, ICardEffect
│   ├── Item/        # 아이템 도메인
│   └── Stage/       # 스테이지 도메인
├── Application/     # 애플리케이션 레이어 (유스케이스)
│   ├── Battle/      # 전투 유스케이스
│   │   ├── StartCombat.cs
│   │   ├── ExecuteCard.cs
│   │   └── EndTurn.cs
│   ├── Character/   # 캐릭터 유스케이스
│   │   ├── InitializeCharacter.cs
│   │   ├── TakeDamage.cs
│   │   └── ApplyEffect.cs
│   ├── Card/        # 카드 유스케이스
│   │   ├── DrawCard.cs
│   │   ├── PlayCard.cs
│   │   └── DiscardCard.cs
│   └── Services/    # 공통 서비스
│       ├── EventBus.cs
│       └── CommandBus.cs
├── Infrastructure/  # 인프라스트럭처 레이어
│   ├── Unity/       # Unity 특화
│   │   ├── MonoBehaviour/
│   │   ├── ScriptableObject/
│   │   └── Coroutine/
│   ├── Persistence/ # 저장/로드
│   │   ├── SaveManager.cs
│   │   └── LoadManager.cs
│   ├── Audio/       # 오디오
│   │   └── AudioManager.cs
│   └── DI/          # DI 바인딩
│       ├── DomainInstaller.cs
│       ├── ApplicationInstaller.cs
│       └── InfrastructureInstaller.cs
└── Presentation/    # 프레젠테이션 레이어
    ├── UI/          # UI 컨트롤러
    │   ├── Battle/  # 전투 UI
    │   ├── Character/ # 캐릭터 UI
    │   └── Common/  # 공통 UI
    ├── VFX/         # VFX 시스템
    └── Input/       # 입력 처리
```

**장점**:
- ✅ 레이어별 책임 명확
- ✅ 기능별로도 분리되어 확장 용이
- ✅ Unity 게임에 적합
- ✅ 테스트 용이
- ✅ 의존성 방향 명확

**단점**:
- ⚠️ 초기 구조 변경 작업량 많음

---

## 🎯 최종 추천: Hybrid 구조 (레이어 + 기능)

### 선택 이유

1. **Unity 게임에 최적화**
   - MonoBehaviour, ScriptableObject 등 Unity 특성을 Infrastructure에 분리
   - 도메인 로직은 Unity 독립적으로 유지

2. **확장성**
   - 새 기능 추가 시 해당 Feature 폴더에만 추가
   - 기존 코드 영향 최소화

3. **테스트 용이성**
   - Domain 레이어는 순수 C# (Unity 의존성 없음)
   - Application 레이어는 인터페이스 기반

4. **유지보수성**
   - 명확한 책임 분리
   - 의존성 방향 명확 (Domain ← Application ← Infrastructure ← Presentation)

---

## 📋 상세 구조 설계

### Domain 레이어 (비즈니스 로직)

```
Domain/
├── Character/
│   ├── Entities/
│   │   ├── Character.cs              # 기본 캐릭터 엔티티
│   │   ├── PlayerCharacter.cs        # 플레이어 캐릭터
│   │   └── EnemyCharacter.cs         # 적 캐릭터
│   ├── ValueObjects/
│   │   ├── CharacterStats.cs         # 캐릭터 스탯
│   │   ├── Resource.cs               # 리소스 (마나, 화살 등)
│   │   └── BuffDebuff.cs             # 버프/디버프
│   └── Interfaces/
│       ├── ICharacter.cs
│       ├── IPlayerCharacter.cs
│       └── IEnemyCharacter.cs
├── Combat/
│   ├── Entities/
│   │   ├── CombatSlot.cs             # 전투 슬롯
│   │   ├── Turn.cs                   # 턴
│   │   └── CombatSession.cs          # 전투 세션
│   ├── ValueObjects/
│   │   ├── CombatPhase.cs            # 전투 페이즈
│   │   ├── SlotPosition.cs           # 슬롯 위치
│   │   └── TurnType.cs               # 턴 타입
│   └── Interfaces/
│       ├── ITurnManager.cs
│       ├── ICombatExecutor.cs
│       └── ISlotRegistry.cs
├── Card/
│   ├── Entities/
│   │   ├── SkillCard.cs              # 스킬 카드
│   │   └── CardEffect.cs             # 카드 효과
│   ├── ValueObjects/
│   │   ├── CardDefinition.cs         # 카드 정의
│   │   └── CardStats.cs              # 카드 스탯
│   └── Interfaces/
│       ├── ISkillCard.cs
│       └── ICardEffect.cs
├── Item/
│   ├── Entities/
│   │   ├── Item.cs                   # 아이템
│   │   └── ItemEffect.cs             # 아이템 효과
│   └── Interfaces/
│       └── IItem.cs
└── Stage/
    ├── Entities/
    │   └── Stage.cs                  # 스테이지
    └── Interfaces/
        └── IStage.cs
```

**특징**:
- ✅ Unity 의존성 없음 (순수 C#)
- ✅ 비즈니스 로직만 포함
- ✅ 인터페이스 기반 설계

---

### Application 레이어 (유스케이스)

```
Application/
├── Battle/
│   ├── StartCombat.cs                # 전투 시작 유스케이스
│   ├── ExecuteCard.cs                # 카드 실행 유스케이스
│   ├── EndTurn.cs                    # 턴 종료 유스케이스
│   └── MoveSlot.cs                   # 슬롯 이동 유스케이스
├── Character/
│   ├── InitializeCharacter.cs        # 캐릭터 초기화
│   ├── TakeDamage.cs                 # 데미지 처리
│   ├── Heal.cs                       # 힐 처리
│   └── ApplyEffect.cs                # 효과 적용
├── Card/
│   ├── DrawCard.cs                   # 카드 뽑기
│   ├── PlayCard.cs                   # 카드 사용
│   ├── DiscardCard.cs                # 카드 버리기
│   └── ShuffleDeck.cs                # 덱 셔플
├── Item/
│   ├── UseItem.cs                    # 아이템 사용
│   └── EquipItem.cs                  # 아이템 장착
└── Services/
    ├── EventBus.cs                   # 이벤트 버스
    ├── CommandBus.cs                 # 명령 버스
    └── QueryBus.cs                   # 쿼리 버스
```

**특징**:
- ✅ 도메인 로직 조합
- ✅ 트랜잭션 관리
- ✅ 이벤트 발행

---

### Infrastructure 레이어 (Unity, 저장, 오디오)

```
Infrastructure/
├── Unity/
│   ├── MonoBehaviour/
│   │   ├── CharacterMonoBehaviour.cs # Unity 컴포넌트 래퍼
│   │   └── CardMonoBehaviour.cs
│   ├── ScriptableObject/
│   │   ├── CharacterDataSO.cs       # ScriptableObject 데이터
│   │   └── CardDataSO.cs
│   └── Coroutine/
│       └── CoroutineRunner.cs
├── Persistence/
│   ├── SaveManager.cs                # 저장 관리
│   ├── LoadManager.cs                # 로드 관리
│   └── SaveData.cs                   # 저장 데이터 모델
├── Audio/
│   ├── AudioManager.cs               # 오디오 관리
│   └── AudioPool.cs                  # 오디오 풀
└── DI/
    ├── DomainInstaller.cs            # Domain 바인딩
    ├── ApplicationInstaller.cs       # Application 바인딩
    └── InfrastructureInstaller.cs    # Infrastructure 바인딩
```

**특징**:
- ✅ Unity 특화 코드
- ✅ 외부 시스템 연동
- ✅ DI 바인딩

---

### Presentation 레이어 (UI, VFX, Input)

```
Presentation/
├── UI/
│   ├── Battle/
│   │   ├── BattleUIController.cs     # 전투 UI
│   │   ├── SlotUI.cs                 # 슬롯 UI
│   │   └── TurnIndicatorUI.cs        # 턴 표시 UI
│   ├── Character/
│   │   ├── CharacterUIController.cs  # 캐릭터 UI
│   │   ├── HealthBarUI.cs            # 체력바 UI
│   │   └── ResourceUI.cs            # 리소스 UI
│   ├── Card/
│   │   ├── CardUI.cs                # 카드 UI
│   │   ├── CardHandUI.cs           # 핸드 UI
│   │   └── CardTooltipUI.cs        # 카드 툴팁
│   └── Common/
│       ├── MainMenuUI.cs            # 메인 메뉴
│       └── SettingsUI.cs            # 설정 UI
├── VFX/
│   ├── VFXManager.cs                # VFX 관리
│   ├── DamageTextVFX.cs             # 데미지 텍스트
│   └── EffectVFX.cs                 # 이펙트 VFX
└── Input/
    ├── InputHandler.cs              # 입력 처리
    └── DragDropHandler.cs           # 드래그앤드롭
```

**특징**:
- ✅ UI 로직만 포함
- ✅ 도메인 로직 없음
- ✅ 이벤트 기반 통신

---

## 🔄 마이그레이션 계획

### Phase 1: Domain 레이어 구축 (1주)

1. **Character 도메인**
   - `Character.cs`, `PlayerCharacter.cs`, `EnemyCharacter.cs` 이동
   - Unity 의존성 제거
   - 인터페이스 정의

2. **Combat 도메인**
   - `CombatSlot.cs`, `Turn.cs` 이동
   - 비즈니스 로직 추출

3. **Card 도메인**
   - `SkillCard.cs`, `CardEffect.cs` 이동
   - 도메인 모델 정리

### Phase 2: Application 레이어 구축 (1주)

1. **유스케이스 작성**
   - 각 기능별 유스케이스 클래스 작성
   - 도메인 서비스 조합

2. **이벤트 시스템**
   - EventBus 구현
   - 도메인 이벤트 정의

### Phase 3: Infrastructure 레이어 구축 (1주)

1. **Unity 어댑터**
   - MonoBehaviour 래퍼 작성
   - ScriptableObject 어댑터

2. **Persistence**
   - SaveManager 재작성
   - 도메인 모델 ↔ 저장 모델 변환

3. **DI 바인딩**
   - 레이어별 Installer 작성

### Phase 4: Presentation 레이어 구축 (1주)

1. **UI 재구성**
   - UI 컨트롤러를 Presentation으로 이동
   - 이벤트 기반 통신

2. **VFX 통합**
   - VFX 시스템을 Presentation으로 이동

### Phase 5: 레거시 코드 제거 (1주)

1. **기존 시스템 폴더 삭제**
   - CharacterSystem, CombatSystem 등 삭제
   - 네임스페이스 정리

2. **테스트 및 검증**
   - 모든 기능 동작 확인
   - 성능 테스트

---

## 📊 구조 비교

| 항목 | 현재 구조 | Hybrid 구조 |
|------|----------|------------|
| **폴더 수** | 11개 시스템 | 4개 레이어 |
| **의존성 방향** | 순환 의존성 | 단방향 의존성 |
| **테스트 용이성** | 어려움 | 쉬움 |
| **확장성** | 중간 | 높음 |
| **코드 재사용** | 낮음 | 높음 |
| **유지보수성** | 중간 | 높음 |

---

## 🎯 네임스페이스 구조

### 새로운 네임스페이스 규칙

```
Game.
├── Domain.                    # 도메인 레이어
│   ├── Character.            # CharacterSystem → Domain.Character
│   │   ├── Entities
│   │   ├── ValueObjects
│   │   └── Interfaces
│   ├── Combat.               # CombatSystem → Domain.Combat
│   ├── Card.                 # SkillCardSystem → Domain.Card
│   ├── Item.                 # ItemSystem → Domain.Item
│   └── Stage.                # StageSystem → Domain.Stage
├── Application.              # 애플리케이션 레이어
│   ├── Battle.
│   ├── Character.
│   ├── Card.
│   └── Services.
├── Infrastructure.           # 인프라스트럭처 레이어
│   ├── Unity.
│   ├── Persistence.
│   ├── Audio.
│   └── DI.
└── Presentation.            # 프레젠테이션 레이어
    ├── UI.
    ├── VFX.
    └── Input.
```

---

## ✅ 장점 요약

1. **명확한 책임 분리**
   - 각 레이어가 명확한 역할
   - 의존성 방향 명확

2. **테스트 용이성**
   - Domain 레이어는 Unity 없이 테스트 가능
   - Mock 객체 사용 용이

3. **확장성**
   - 새 기능 추가 시 해당 Feature 폴더에만 추가
   - 기존 코드 영향 최소화

4. **유지보수성**
   - 코드 위치 예측 가능
   - 버그 수정 범위 명확

5. **재사용성**
   - 도메인 로직 재사용 가능
   - 다른 프로젝트로 이식 용이

---

## 🔗 관련 문서

- [완전 재작성 리팩토링 계획](./CompleteRefactoringPlan.md)
- [스크립트 상세 분석](./DetailedScriptAnalysis.md)
- [전체 스크립트 체크리스트](./CompleteScriptChecklist.md)

